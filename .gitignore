/js-example/dist
/js-example/node_modules
/js-example/pkg

/target
/result
/sav
/.direnv
/.idea
last_log.log
last_test_log.log
# The engine log and the input log both live in `<game>/logs`, and a game
# directory can be anywhere — including the fixtures under `crates/*/tests`.
logs/
massif*
*~undo-tree~
heaptrack*
game.era
# proprietary font kept locally for the opt-in MS Gothic tests (ERARS_FONT_DIR); never commit
msgothic.ttc
.DS_Store
__pycache__/
/2026-*.txt
