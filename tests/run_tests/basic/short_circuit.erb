@SYSTEM_TITLE
IF 0 && SIDE_EFFECT_NOT()
ELSEIF 0 & SIDE_EFFECT_OK()
ENDIF

IF !(1 || SIDE_EFFECT_NOT())
ELSEIF !(1 | SIDE_EFFECT_OK())
ENDIF

@SIDE_EFFECT_NOT
#FUNCTION
THROW Shouldn't be called

@SIDE_EFFECT_OK
#FUNCTION
PRINTL Should be called
RETURNF 1
